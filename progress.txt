# DevOps 平台项目进展跟踪 (Project Progress Tracker)

## 🎯 当前状态 (Current Focus)
[阶段：执行中]
分支：feat/qa-gitlab-style-ui
任务：完成测试用例模块 UI 的交互细节优化（步骤检查、回执评论），并同步 Ruff 规则至全量代码。

## 🏗️ 进行中 (In Progress)

- [x] [P1] 测试用例模块 UI 重构与交互升级 (GitLab 风格) (2026-02-23)
    - 结果：成功复刻 GitLab 列表+详情双栏布局，并新增交互反馈。
    - 实现：
        1. 引入 Web Components 实现双栏同步。
        2. 支持测试步骤交互式 Checkbox 标记与实时进度条。
        3. 增加执行评论回传与环境标识，打通后端全链路执行 API。
    - 分支：开发于 `feat/qa-gitlab-style-ui` 分支，验证通过。
📅 接下来 (Backlog)

- [ ] [P1] 待禅道服务器上线后执行端到端全量数据同步验证 (2026-02-20)

## 📋 最近完成 (Recently Completed)

1. **[P3] Ruff 极致性能工具链迁移实验 (isort 整合)** (2026-02-23)
    - 成果：替代原有 `isort` + `flake8-import-order`。单次运行全量（266 文件）排序修复耗时 < 0.5s。
    - 风险控制：通过 `[lint.per-file-ignores]` 机制保护了 `__init__.py` 中的插件注册逻辑，解决了初始化导致的 `ModuleNotFoundError`。
    - 配置：新增 `ruff.toml` 影子运行，已执行 `ruff check --select I --fix` 完成全量 Import 规范化。
    - 场景：录入测试用例与产品选择时，部门选择框原为扁平显示，缺少层级关系。另外 API 中存在冗余。
    - 优化：通过前端获取 `org_level` 后，在原生的 `<select>` 中动态拼接 `├─ ` 缩进前缀实现直观的树形视觉展示，兼顾了改动最小原则。同时删除了 `admin_router.py` 中重复的（无层次信息的） `/organizations` 冗余路由。
    - 测试：`pytest tests/unit/portal/test_admin_router.py` 验证通过。


1. **[P1] 实现 Portal UI 职能分离 (Separation of Concerns)** (2026-02-21)
    - 场景：质量看板 (Dashboard) vs 测试用例 (Test Cases)。
    - 优化：在“质量看板”视图下自动隐藏“仓库运维工具栏”，并将其标题切换为“Quality Dashboard”。
    - 意义：减少看板页面的干扰项，强化“洞察”与“管理”的界面区隔。
    - 适配文件：`index.html` (增加 DOM ID), `sys_app.js` (视图状态维护)。

2. **[P1] 实现本地缓存优先机制 (Local First Cache)** (2026-02-21)
    - 镜像缓存：修改 `Makefile`，在拉取前检查本地是否存在基础镜像，存在则跳过 Pull。
    - 依赖缓存：启用 Docker BuildKit `cache mount`，在 `Dockerfile` 中持久化 `pip` 和 `apt` 缓存，避免重复下载。
    - 验证：第二次执行 `make build` 时，基础镜像检测为 `skipping pull`，依赖安装层直接使用缓存，构建效率显著提升。

2. **[P1] 优化容器部署流水线：Nexus 双层回退算法 (Python & Docker)** (2026-02-21)
    - 实现逻辑 (Python): Nexus 私服 (无鉴权) -> 官方 PyPI -> 清华镜像
    - 实现逻辑 (Docker): Nexus Docker Registry (192.168.1.168:8082, 无鉴权) -> 官方 Docker Hub
    - 适配范围：`Dockerfile`, `Makefile` (build, package, pull-images), `.env.example`
    - 覆盖对象：`python:3.11-slim-bookworm`, `postgres:15-alpine`, `rabbitmq:3-management-alpine`
    - 验证：`make deploy` 在 VPN 环境下通过 (耗时约 15min)，fallback 机制在 Nexus 离线时自动切换官方源成功。

2. **[P1] 验证多环境网络适配性** (2026-02-21)
    - 验证：已通过 `make deploy` 完整部署流程。在 VPN 开启状态下，虽然 Nexus 离线导致 fallback，但后续官方源下载与构建成功。
    - 改进：在 `Dockerfile` 和 `Makefile` 中增加了更鲁棒的 timeout 和 fallback 策略。

2. **[P0] 强化 AI 协作规范 ( gemini.md & contexts.md )** (2026-02-21)
    - 引入“显式状态锁定”、“严格问答解耦”及“原子步审计”机制
    - 目的：杜绝未决策先执行（抢跑）行为，确保意图与实现强对齐
    - 验证：规范文档已同步更新，AI 内部审计逻辑已激活

3. **[P3] 修复 7 个各类 ImportError 的损坏测试文件** (2026-02-18)
    - 修复 `test_analyzer.py` (DiffAnalyzer -> CodeMetrics)
    - 修复 `test_gitlab_worker.py` (IdentityMatcher 导入路径)
    - 修复 `okr_service.py` 及测试 (GitLab 模型导入)
    - 修复 `test_project_management.py` (SysRole 别名)
    - 修复 3 个 reliability 测试 (GitLabClient 路径及模型导入)
    - 验证：`pytest --collect-only` 全量通过，无 ImportError

2. **[P1] 全局开发规范优化 (gemini.md Overhaul)** (2026-02-18)
    - 第十二章 "AI 原生协作哲学" 从 5 条扩展为 10 条原则 + 核心公式
    - 新增原则：意图驱动、反馈环密度、组合式架构、信任但验证、演进式设计
    - 第一至十一章全面优化：统一层级编号、补充可观测性/DB Migration/环境变量规范
    - progress.txt 规则升级为"固定四段式 + 归档机制"

3. **[P2] 灵活配置管理与注册域名白名单** (2026-02-18)
    - 将子配置类从 `BaseSettings` 转为 `BaseModel`，引入 `Union[str, List[str]]` 解决 Pydantic V2 解析问题
    - 在 `.env` 引入 `AUTH__ALLOWED_DOMAINS`，支持开放模式

4. **[P1] Nexus 深度集成与 MDM 自动对齐** (2026-02-18)
    - 模型标准化改造（TimestampMixin, SCDMixin, OwnableMixin）
    - 智能对齐：根据 `group`/`name` 自动解析 MDM 产品代码
    - 验证：多场景单元测试全部通过 ✅

5. **[P1] Unified Exception System & Audit Persistence** (2026-02-18)
    - 实现 `BusinessException` 层级体系 + FastAPI 全局异常处理器
    - 安全模型增加 TimestampMixin/SCDMixin/created_by 审计字段
    - 验证：所有测试 100% 通过

## 📝 阶段性回顾 (Retrospective)

- **技术突破**：利用 Pydantic 的 `Union` 类型绕过 `DotEnvSettingsSource` 的强 JSON 限制
- **流程合规**：遵循"反向采访-用户决策-原子开发-质量验证"闭环
- **质量文化**：由被动修 Bug 转为主动清偿技术债 (P3 broken tests)，确保 CI 基础稳固

> 更早的已完成任务已归档至 `progress_archive.md`（共 15 条）

---
*最后更新时间：2026-02-23 11:30 (Antigravity)*
